package com.poorva.tic_tac_toe;

/**
 * This class contains only the constants which are used in different places in the project
 */
public class Constants {
    // Window width and height
    public static final int WINDOW_HEIGHT = 400;
    public static final int WINDOW_WIDTH = 300;

    // Header part height. Width is same as the window
    public static final int HEADER_HEIGHT = 100;

    // Tiles board height. Width is same as the window
    public static final int BOARD_HEIGHT = 300;
}
